# Importing squares from functions.py
from functions import square

# Importing the square function from function.py and looping it to get squares
for i in range(10):
    print(f"The square of {i} is {square(i)}")

#I can also import by the this(below)
# import functions
# But if I do this(Above) If have to write in print(f"The square of {i} is {functions.square(i)}")

